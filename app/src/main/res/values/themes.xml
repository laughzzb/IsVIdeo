<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FristProjectVideo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FristProjectVideo" parent="Base.Theme.FristProjectVideo" />
    android:button="@null"
    android:gravity="center"
    android:drawableTop="@mipmap/icon_home"
    android:paddingTop="10dp"
    <style name="maiBottomRadioButton">
        <item name="android:button">@null</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingTop">10dp</item>
        <item name="android:textColor">@drawable/radio_bottom_text_color</item>
    </style>

    <style name="SplashTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
    <item name="android:windowBackground">@drawable/background</item>
    </style>
</resources>